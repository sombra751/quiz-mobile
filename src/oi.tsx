import React from 'react';
import { View, Text } from 'react-native';

const OiComponent = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 24, fontWeight: 'bold' }}>Oi</Text>
    </View>
  );
};

export default OiComponent;